// src/common/enums/elementals/login-stats.ts

/**
 * Enum de estados de login
 * @returns {string} retorna enum de estados de login
 */
export enum LoginStatsEnum {
  NOT_VERIFIELD = 'NOT_VERIFIELD',
  AVAILABLE = 'AVAILABLE',
  LOCKED = 'LOCKED',
  IN_RECOVERY = 'IN_RECOVERY',
}
